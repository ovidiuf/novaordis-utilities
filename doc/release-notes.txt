* Major XML API change: Xml -> XML, incompatible API.
* OS
  * multi-threaded consumption of stdout and stderr threads, removes potential for deadlock on small stream storage
    capacity.

* Introduced package io.novaordis.utilities.env, EnvironmentVariableProvider and the mechanism that allows for a
  configurable EnvironmentVariableProvider.

* Files: introduced Files.normalizePath()

* Added async-collector under io.novaordis.utilities.ac